# Flask and WSGI
Flask
Werkzeug
gunicorn

# PostgreSQL
psycopg2-binary

# Environment variables
python-dotenv

# HMAC and cryptography (for signature validation)
cryptography
byteforge-hmac

# Monitoring and observability
prometheus-client
python-json-logger

# Rate limiting
redis

# Centralized logging with Loki
byteforge-loki-logging @ git+https://${CR_PAT}@github.com/jmazzahacks/byteforge-loki-logging.git

# Shared models
api-gatekeeper-models @ git+https://github.com/jmazzahacks/api-gatekeeper-models.git

# Aegis client (bearer token introspection via /api/auth/me).
# Pinned to the "v2.0.0" commit (phase-3 UUID-only contract). The client
# repo advertises version = "2.0.0" in its pyproject but the maintainer
# forgot to cut a matching git tag, so we pin the SHA to keep the build
# reproducible. Once a v2.0.0 tag lands, switch this back to @v2.0.0.
# Version 2.0.0 REQUIRES the contracted Aegis backend — do not run mixed
# versions. Transitively pins byteforge-aegis-models @ v2.0.0.
byteforge-aegis-client-python @ git+https://github.com/jmazzahacks/byteforge-aegis-client-python.git@bf50171911c501621eb7f9b770bacc4e93764f72
